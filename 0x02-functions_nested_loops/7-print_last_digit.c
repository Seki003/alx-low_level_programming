#include <"main.h">

/**
 * print_last_digit - checks the last character of a number
 * @n: the number to be checked
 * Return: 0 after giving the last digit of n
 */
int print_last_digit(int n)
{
	_putchar("%d", n % 10);
    return (0);
}
